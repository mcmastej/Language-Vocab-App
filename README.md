# V5.6 Korean Memory Game — POS label below image

This build keeps the working V5.5 layout and flat-file image setup.

Change:
- `(noun)`, `(verb)`, and `(adjective)` are centered directly below the presented image instead of appearing beside it.

All game mechanics remain unchanged.
All 10 SVG files remain at the repository root for individual GitHub upload.
The service worker remains disabled during testing.
